<Window x:Class="RasFocusPro.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="RasFocus Pro" Height="780" Width="1180"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen" MouseLeftButtonDown="Window_MouseLeftButtonDown"
        TextOptions.TextFormattingMode="Display" TextOptions.TextRenderingMode="ClearType">

    <Window.Resources>
        <SolidColorBrush x:Key="PrimaryTeal" Color="#14B8C8"/>
        <SolidColorBrush x:Key="PrimaryTealHover" Color="#11A1B0"/>
        <SolidColorBrush x:Key="DarkText" Color="#333333"/>
        <SolidColorBrush x:Key="LightBg" Color="#FFFFFF"/>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource LightBg}"/>
            <Setter Property="Foreground" Value="{StaticResource DarkText}"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="BorderBrush" Value="#CBD5E1"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border CornerRadius="4" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="0"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter Property="BorderBrush" Value="{StaticResource PrimaryTeal}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Button" x:Key="TealBtn">
            <Setter Property="Background" Value="{StaticResource PrimaryTeal}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="5" Padding="20,8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource PrimaryTealHover}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Button" x:Key="CoralBtn" BasedOn="{StaticResource TealBtn}">
            <Setter Property="Background" Value="#FF5454"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E54B4B"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Button" x:Key="PresetBtn">
            <Setter Property="Background" Value="#DDF4F6"/>
            <Setter Property="Foreground" Value="#14B8C8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="15" Padding="25,8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource PrimaryTeal}"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="CheckBox" x:Key="ToggleSwitch">
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <StackPanel Orientation="Horizontal">
                            <Grid Width="46" Height="24">
                                <Border x:Name="BgBorder" CornerRadius="12" Background="#D1D5DB"/>
                                <Ellipse x:Name="Thumb" Fill="White" Width="20" Height="20" HorizontalAlignment="Left" Margin="2">
                                    <Ellipse.Effect>
                                        <DropShadowEffect BlurRadius="4" ShadowDepth="1" Opacity="0.2"/>
                                    </Ellipse.Effect>
                                </Ellipse>
                            </Grid>
                            <ContentPresenter Margin="15,0,0,0" VerticalAlignment="Center" TextBlock.FontSize="15" TextBlock.Foreground="{StaticResource DarkText}"/>
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="BgBorder" Property="Background" Value="{StaticResource PrimaryTeal}"/>
                                <Setter TargetName="Thumb" Property="HorizontalAlignment" Value="Right"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="RadioButton" x:Key="TabBtn">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#888888"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="0,0,0,2" Padding="0,10,0,10" Background="Transparent">
                            <ContentPresenter HorizontalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsChecked" Value="True">
                    <Setter Property="Foreground" Value="{StaticResource PrimaryTeal}"/>
                    <Setter Property="BorderBrush" Value="{StaticResource PrimaryTeal}"/>
                </Trigger>
                <Trigger Property="IsChecked" Value="False">
                    <Setter Property="BorderBrush" Value="#E2E8F0"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Border Background="{StaticResource LightBg}" CornerRadius="10" Margin="15">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="4" Opacity="0.15" Color="Black"/>
        </Border.Effect>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="230"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="{StaticResource PrimaryTeal}" CornerRadius="10,0,0,10">
                <StackPanel Margin="0,25,0,0">
                    <StackPanel Orientation="Horizontal" Margin="20,0,0,40">
                        <TextBlock Text="👁️" FontSize="26" Foreground="White" Margin="0,0,10,0"/>
                        <TextBlock Text="RasFocus Pro" Foreground="White" FontSize="22" FontWeight="Bold" VerticalAlignment="Center"/>
                    </StackPanel>

                    <ListBox x:Name="SidebarList" Background="Transparent" BorderThickness="0" Foreground="White" FontSize="16" FontWeight="SemiBold" SelectionChanged="SidebarList_SelectionChanged">
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="Padding" Value="25,18"/>
                                <Setter Property="Cursor" Value="Hand"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListBoxItem">
                                            <Border x:Name="BorderBg" Background="Transparent">
                                                <Border x:Name="InnerBorder" BorderThickness="0" BorderBrush="Transparent">
                                                    <ContentPresenter Margin="{TemplateBinding Padding}"/>
                                                </Border>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsSelected" Value="True">
                                                    <Setter TargetName="BorderBg" Property="Background" Value="{StaticResource LightBg}"/>
                                                    <Setter Property="Foreground" Value="{StaticResource PrimaryTeal}"/>
                                                </Trigger>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="BorderBg" Property="Background" Value="#17C3D4"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListBox.ItemContainerStyle>
                        <ListBoxItem IsSelected="True">💻  Display / Eye Cure</ListBoxItem>
                        <ListBoxItem>☕  Pomodoro Break</ListBoxItem>
                        <ListBoxItem>🛡️  Focus Mode</ListBoxItem>
                        <ListBoxItem>⚙️  Settings</ListBoxItem>
                        <ListBoxItem>💬  Live Chat</ListBoxItem>
                        <ListBoxItem>⭐  Activate Pro</ListBoxItem>
                    </ListBox>
                </StackPanel>
            </Border>

            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="50"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="10">
                    <Button Content="?" Background="Transparent" Foreground="#888" FontSize="16" BorderThickness="0" Width="35" Height="35" Cursor="Hand"/>
                    <Button Content="✉" Background="Transparent" Foreground="#888" FontSize="18" BorderThickness="0" Width="35" Height="35" Cursor="Hand"/>
                    <Button Content="—" Background="Transparent" Foreground="#888" FontSize="16" BorderThickness="0" Width="35" Height="35" Cursor="Hand" Click="CloseButton_Click"/>
                    <Button Content="✕" Background="Transparent" Foreground="#888" FontSize="16" BorderThickness="0" Width="35" Height="35" Cursor="Hand" Click="CloseButton_Click"/>
                </StackPanel>

                <StackPanel x:Name="PageEyeCure" Grid.Row="1" Margin="50,20,50,40" Visibility="Visible">
                    
                    <Grid Margin="0,0,0,40">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <RadioButton Grid.Column="0" Content="Focus Read" Style="{StaticResource TabBtn}" IsChecked="True"/>
                        <RadioButton Grid.Column="1" Content="Focus Blur" Style="{StaticResource TabBtn}"/>
                    </Grid>

                    <TextBlock Text="Adjust color temperature and brightness to prevent eye strain." FontSize="15" Foreground="{StaticResource DarkText}" Margin="0,0,0,40" TextWrapping="Wrap"/>

                    <Grid Margin="0,0,0,40">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="50"/>
                            <RowDefinition Height="50"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Warm" VerticalAlignment="Center" FontSize="15" Foreground="{StaticResource DarkText}"/>
                        <Slider Grid.Row="0" Grid.Column="1" Maximum="100" Value="60" VerticalAlignment="Center" Margin="10,0"/>

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="Dimmer" VerticalAlignment="Center" FontSize="15" Foreground="{StaticResource DarkText}"/>
                        <Slider Grid.Row="1" Grid.Column="1" Maximum="100" Value="68" VerticalAlignment="Center" Margin="10,0"/>
                    </Grid>

                    <Border BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="20" Padding="15,10" Margin="0,0,0,40" Background="White" HorizontalAlignment="Left" Width="450">
                        <StackPanel Orientation="Horizontal">
                            <CheckBox Content="Auto Day/Night" Style="{StaticResource ToggleSwitch}" Margin="0,0,60,0"/>
                            <Border Background="#F1F5F9" CornerRadius="15">
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="Day" Background="White" Foreground="{StaticResource PrimaryTeal}" FontWeight="Bold" BorderThickness="0" Padding="20,5" Margin="2" Cursor="Hand"/>
                                    <Button Content="Night" Background="Transparent" Foreground="#888" FontWeight="Bold" BorderThickness="0" Padding="20,5" Margin="2" Cursor="Hand"/>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </Border>

                    <WrapPanel Margin="0,0,0,20">
                        <Button Content="Pause" Style="{StaticResource PresetBtn}" Margin="0,0,15,15" Width="100"/>
                        <Button Content="Health" Style="{StaticResource PresetBtn}" Margin="0,0,15,15" Width="100" Background="{StaticResource PrimaryTeal}" Foreground="White"/>
                        <Button Content="Game" Style="{StaticResource PresetBtn}" Margin="0,0,15,15" Width="100"/>
                        <Button Content="Movie" Style="{StaticResource PresetBtn}" Margin="0,0,15,15" Width="100"/>
                        <Button Content="Office" Style="{StaticResource PresetBtn}" Margin="0,0,15,15" Width="100"/>
                        <Button Content="Editing" Style="{StaticResource PresetBtn}" Margin="0,0,15,15" Width="100"/>
                        <Button Content="Reading" Style="{StaticResource PresetBtn}" Margin="0,0,15,15" Width="100"/>
                    </WrapPanel>
                </StackPanel>

                <StackPanel x:Name="PagePomodoro" Grid.Row="1" Margin="50,10,50,40" Visibility="Collapsed">
                    <Border CornerRadius="8" Background="White" BorderBrush="#E2E8F0" BorderThickness="1">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.1"/>
                        </Border.Effect>
                        <StackPanel>
                            <Border Background="{StaticResource PrimaryTeal}" CornerRadius="8,8,0,0" Padding="0,40,0,50">
                                <StackPanel HorizontalAlignment="Center">
                                    <TextBlock Text="Next break in:" Foreground="White" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center"/>
                                    <TextBlock x:Name="LblPomoTime" Text="00:00:00" Foreground="White" FontSize="90" FontFamily="Segoe UI Light" HorizontalAlignment="Center" Margin="0,10"/>
                                    <Button x:Name="BtnPomoStart" Content="START" Style="{StaticResource CoralBtn}" FontSize="22" Padding="60,12" Click="BtnPomoStart_Click"/>
                                </StackPanel>
                            </Border>

                            <StackPanel Margin="40,30">
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                                    <TextBlock Text="Pomodoro Technique Mode" FontSize="16" Foreground="{StaticResource DarkText}" VerticalAlignment="Center" Width="250"/>
                                    <CheckBox Style="{StaticResource ToggleSwitch}"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                                    <TextBlock Text="Focus Duration" FontSize="16" Foreground="{StaticResource DarkText}" VerticalAlignment="Center" Width="250"/>
                                    <TextBox Text="25" Width="60" TextAlignment="Center"/>
                                    <TextBlock Text="Minutes" FontSize="15" Foreground="{StaticResource DarkText}" VerticalAlignment="Center" Margin="10,0,0,0"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="Rest Duration" FontSize="16" Foreground="{StaticResource DarkText}" VerticalAlignment="Center" Width="250"/>
                                    <TextBox Text="5" Width="60" TextAlignment="Center"/>
                                    <TextBlock Text="Minutes" FontSize="15" Foreground="{StaticResource DarkText}" VerticalAlignment="Center" Margin="10,0,0,0"/>
                                    <Button x:Name="BtnPomoStop" Content="Stop Timer" Background="Transparent" Foreground="#EF4444" BorderThickness="0" FontSize="16" FontWeight="Bold" Cursor="Hand" Margin="20,0,0,0" Click="BtnPomoStop_Click"/>
                                </StackPanel>
                                <TextBlock x:Name="LblPomoStatus" Text="Ready" Margin="0,20,0,0" FontWeight="Bold" Foreground="{StaticResource PrimaryTeal}" HorizontalAlignment="Center" />
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <StackPanel x:Name="PageFocusMode" Grid.Row="1" Margin="40,10,40,40" Visibility="Collapsed">
                    <TextBlock Text="App &amp; Website Blocker" FontSize="24" FontWeight="Bold" Foreground="{StaticResource PrimaryTeal}" Margin="0,0,0,20"/>
                    
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                        <TextBlock Text="Profile:" VerticalAlignment="Center" FontSize="15" FontWeight="Bold" Foreground="{StaticResource DarkText}" Margin="0,0,10,0"/>
                        <TextBox x:Name="TxtProfileName" Width="180" Margin="0,0,15,0" Text="Rasel Mia"/>
                        <Button Content="Save" Style="{StaticResource TealBtn}" Padding="15,6" Click="BtnSave_Click"/>
                        <TextBlock x:Name="LblStatus" Text="Ready" VerticalAlignment="Center" FontWeight="Bold" Foreground="{StaticResource PrimaryTeal}" Margin="20,0,0,0" />
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                        <TextBlock Text="Friend Pass:" VerticalAlignment="Center" FontSize="15" FontWeight="Bold" Foreground="{StaticResource DarkText}" Margin="0,0,10,0"/>
                        <TextBox x:Name="TxtPass" Width="120" Margin="0,0,20,0"/>
                        <Button x:Name="BtnStart" Content="LOCK DEVICE" Style="{StaticResource CoralBtn}" Padding="20,6" Click="BtnStart_Click"/>
                        <Button x:Name="BtnStop" Content="STOP" Style="{StaticResource TealBtn}" Background="#64748B" Margin="10,0,0,0" Click="BtnStop_Click"/>
                    </StackPanel>

                    <WrapPanel Margin="0,0,0,20">
                        <CheckBox Content="Ad Blocker" Style="{StaticResource ToggleSwitch}" Margin="0,0,30,10"/>
                        <CheckBox Content="Block FB Reels" Style="{StaticResource ToggleSwitch}" Margin="0,0,30,10"/>
                        <CheckBox Content="Block YT Shorts" Style="{StaticResource ToggleSwitch}" Margin="0,0,30,10"/>
                    </WrapPanel>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="20"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Border Grid.Column="0" Background="White" CornerRadius="8" BorderBrush="#E2E8F0" BorderThickness="1" Padding="15">
                            <StackPanel>
                                <TextBlock Text="Blocked Apps (e.g., vlc.exe)" FontWeight="Bold" FontSize="15" Foreground="{StaticResource DarkText}" Margin="0,0,0,10"/>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                    <TextBox x:Name="TxtAppAdd" Width="180" Margin="0,0,10,0"/>
                                    <Button Content="Add" Style="{StaticResource TealBtn}" Padding="15,5" Click="BtnAddApp_Click"/>
                                </StackPanel>
                                <ListBox x:Name="ListBlockedApps" Height="170" BorderBrush="#CBD5E1" FontSize="15" Foreground="{StaticResource DarkText}"/>
                                <Button Content="Remove Selected" Background="#EF4444" Foreground="White" BorderThickness="0" Padding="10" Margin="0,10,0,0" Cursor="Hand" Click="BtnRemoveApp_Click"/>
                            </StackPanel>
                        </Border>

                        <Border Grid.Column="2" Background="White" CornerRadius="8" BorderBrush="#E2E8F0" BorderThickness="1" Padding="15">
                            <StackPanel>
                                <TextBlock Text="Running Apps (Auto-Detect)" FontWeight="Bold" FontSize="15" Foreground="{StaticResource DarkText}" Margin="0,0,0,10"/>
                                <ListBox x:Name="ListRunningApps" Height="170" BorderBrush="#CBD5E1" FontSize="14" Foreground="{StaticResource DarkText}"/>
                                <Button Content="Refresh List" Style="{StaticResource TealBtn}" Padding="10" Margin="0,10,0,0" Click="BtnRefresh_Click"/>
                            </StackPanel>
                        </Border>
                    </Grid>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>
