<Window x:Class="RasFocusPro.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="RasFocus Pro" Height="720" Width="1100"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen" MouseLeftButtonDown="Window_MouseLeftButtonDown"
        TextOptions.TextFormattingMode="Display" TextOptions.TextRenderingMode="ClearType">

    <Window.Resources>
        <SolidColorBrush x:Key="PrimaryTeal" Color="#0EA5E9"/> 
        <SolidColorBrush x:Key="DarkText" Color="#1E293B"/>
        <SolidColorBrush x:Key="LightBg" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="CardBg" Color="#F8FAFC"/>
        <SolidColorBrush x:Key="BorderColor" Color="#CBD5E1"/>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource LightBg}"/>
            <Setter Property="Foreground" Value="{StaticResource DarkText}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border CornerRadius="6" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="0"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter Property="BorderBrush" Value="{StaticResource PrimaryTeal}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="{StaticResource LightBg}"/>
            <Setter Property="Foreground" Value="{StaticResource DarkText}"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="6,4"/>
        </Style>

        <Style TargetType="Button" x:Key="CareBtn">
            <Setter Property="Background" Value="{StaticResource PrimaryTeal}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8" Padding="12,8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Opacity" Value="0.85"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Opacity" Value="0.7"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="CheckBox" x:Key="ToggleSwitch">
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <StackPanel Orientation="Horizontal">
                            <Grid Width="40" Height="20">
                                <Border x:Name="BgBorder" CornerRadius="10" Background="#D1D5DB"/>
                                <Ellipse x:Name="Thumb" Fill="White" Width="16" Height="16" HorizontalAlignment="Left" Margin="2">
                                    <Ellipse.Effect>
                                        <DropShadowEffect BlurRadius="4" ShadowDepth="1" Opacity="0.2"/>
                                    </Ellipse.Effect>
                                </Ellipse>
                            </Grid>
                            <ContentPresenter Margin="8,0,0,0" VerticalAlignment="Center" TextBlock.FontSize="13" TextBlock.FontWeight="SemiBold" TextBlock.Foreground="{StaticResource DarkText}"/>
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="BgBorder" Property="Background" Value="#10B981"/> 
                                <Setter TargetName="Thumb" Property="HorizontalAlignment" Value="Right"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style TargetType="ListBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="{StaticResource DarkText}"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="ItemContainerStyle">
                <Setter.Value>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Padding" Value="5,3"/>
                        <Setter Property="Margin" Value="0,2"/>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#E0F2FE"/>
                                <Setter Property="Foreground" Value="#0369A1"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="{StaticResource CardBg}" CornerRadius="15" Margin="15">
        <Border.Effect>
            <DropShadowEffect BlurRadius="25" ShadowDepth="5" Opacity="0.2" Color="Black"/>
        </Border.Effect>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="240"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="{StaticResource PrimaryTeal}" CornerRadius="15,0,0,15">
                <StackPanel Margin="0,25,0,0">
                    <StackPanel Orientation="Horizontal" Margin="25,0,0,40">
                        <TextBlock Text="🛡️" FontSize="28" Foreground="White" Margin="0,0,10,0"/>
                        <TextBlock Text="RasFocus Pro" Foreground="White" FontSize="20" FontWeight="Bold" VerticalAlignment="Center"/>
                    </StackPanel>

                    <ListBox x:Name="SidebarList" Background="Transparent" BorderThickness="0" Foreground="White" FontSize="15" FontWeight="SemiBold" SelectionChanged="SidebarList_SelectionChanged">
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="Padding" Value="25,15"/>
                                <Setter Property="Cursor" Value="Hand"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListBoxItem">
                                            <Border x:Name="BorderBg" Background="Transparent" CornerRadius="0,25,25,0" Margin="0,0,20,5">
                                                <ContentPresenter Margin="{TemplateBinding Padding}"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsSelected" Value="True">
                                                    <Setter TargetName="BorderBg" Property="Background" Value="#FFFFFF"/>
                                                    <Setter Property="Foreground" Value="{StaticResource PrimaryTeal}"/>
                                                </Trigger>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="BorderBg" Property="Background" Value="#38BDF8"/>
                                                    <Setter Property="Foreground" Value="White"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListBox.ItemContainerStyle>
                        <ListBoxItem IsSelected="True">🚀  Focus Mode</ListBoxItem>
                        <ListBoxItem>💻  Display / Eye Cure</ListBoxItem>
                        <ListBoxItem>🍅  Pomodoro Break</ListBoxItem>
                        <ListBoxItem>⚙️  Settings</ListBoxItem>
                        <ListBoxItem>💬  Live Chat</ListBoxItem>
                        <ListBoxItem>⭐  Activate Pro</ListBoxItem>
                    </ListBox>
                </StackPanel>
            </Border>

            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="40"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="10,10,15,0">
                    <Button Content="—" Background="Transparent" Foreground="#94A3B8" FontSize="18" FontWeight="Bold" BorderThickness="0" Width="35" Height="35" Cursor="Hand"/>
                    <Button Content="✕" Background="Transparent" Foreground="#EF4444" FontSize="18" FontWeight="Bold" BorderThickness="0" Width="35" Height="35" Cursor="Hand" Click="CloseButton_Click"/>
                </StackPanel>

                <StackPanel x:Name="PageFocusMode" Grid.Row="1" Margin="25,0,25,15" Visibility="Visible">
                    
                    <Grid Margin="0,0,0,15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Orientation="Horizontal">
                            <TextBlock Text="Profile Name:" VerticalAlignment="Center" FontSize="14" FontWeight="SemiBold" Foreground="#475569" Margin="0,0,10,0"/>
                            <TextBox x:Name="TxtProfileName" Width="140" Margin="0,0,10,0" Text="Rasel Mia"/>
                            <Button x:Name="BtnSaveProfile" Content="SAVE" Style="{StaticResource CareBtn}" Background="#10B981" Padding="15,6" FontSize="12" Click="BtnSave_Click"/>
                        </StackPanel>

                        <TextBlock x:Name="LblTrialStatus" Grid.Column="1" Text="TRIAL: 7 DAYS LEFT" Foreground="#F59E0B" FontWeight="Bold" FontSize="15" VerticalAlignment="Center" HorizontalAlignment="Center"/>

                        <StackPanel Grid.Column="2" Orientation="Horizontal">
                            <Button x:Name="BtnLiveChatShortcut" Content="LIVE CHAT" Style="{StaticResource CareBtn}" Background="#EC4899" Margin="0,0,10,0" Padding="15,6" FontSize="12" Click="BtnNavLiveChat_Click"/>
                            <Button x:Name="BtnUpgradeShortcut" Content="UPGRADE" Style="{StaticResource CareBtn}" Background="#F59E0B" Padding="15,6" FontSize="12" Click="BtnNavUpgrade_Click"/>
                        </StackPanel>
                    </Grid>

                    <Border Background="White" CornerRadius="10" Padding="15" Margin="0,0,0,15" BorderBrush="#E2E8F0" BorderThickness="1">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.05"/>
                        </Border.Effect>
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                                <TextBlock Text="Pass:" VerticalAlignment="Center" FontSize="14" FontWeight="SemiBold" Foreground="#475569" Margin="0,0,8,0"/>
                                <TextBox x:Name="TxtPass" Width="90" Margin="0,0,20,0"/>

                                <TextBlock Text="Time (Hr:Min):" VerticalAlignment="Center" FontSize="14" FontWeight="SemiBold" Foreground="#475569" Margin="0,0,8,0"/>
                                <TextBox x:Name="TxtTimeHr" Width="45" Margin="0,0,5,0" Text="0"/>
                                <TextBox x:Name="TxtTimeMin" Width="45" Margin="0,0,20,0" Text="0"/>

                                <Button x:Name="BtnStartFocus" Content="START" Style="{StaticResource CareBtn}" Background="#10B981" Margin="0,0,10,0" Click="BtnStart_Click"/>
                                <Button x:Name="BtnStopFocus" Content="STOP" Style="{StaticResource CareBtn}" Background="#EF4444" Margin="0,0,10,0" Click="BtnStop_Click"/>
                                
                                <Button x:Name="BtnNavPomodoro" Content="POMODORO" Style="{StaticResource CareBtn}" Background="#F43F5E" Margin="0,0,10,0" Click="BtnNavPomodoro_Click"/>
                                <Button x:Name="BtnNavEyeCure" Content="EYE CURE" Style="{StaticResource CareBtn}" Background="#8B5CF6" Margin="0,0,20,0" Click="BtnNavEyeCure_Click"/>

                                <TextBlock x:Name="LblStatus" Text="Ready" Foreground="#EF4444" FontSize="15" FontWeight="Bold" VerticalAlignment="Center"/>
                            </StackPanel>

                            <WrapPanel>
                                <RadioButton x:Name="RadioBlockList" Content="Block List" IsChecked="True" Foreground="#1E293B" FontWeight="Bold" FontSize="14" Margin="0,0,25,0" VerticalAlignment="Center" Checked="RadioList_Checked"/>
                                <RadioButton x:Name="RadioAllowList" Content="Allow List (Runs in Pomodoro)" Foreground="#1E293B" FontWeight="Bold" FontSize="14" Margin="0,0,35,0" VerticalAlignment="Center" Checked="RadioList_Checked"/>
                                
                                <CheckBox x:Name="ChkAdBlock" Content="AD BLOCKER" Style="{StaticResource ToggleSwitch}" Margin="0,0,25,0"/>
                                <CheckBox x:Name="ChkBlockReels" Content="Block FB Reels" Style="{StaticResource ToggleSwitch}" Margin="0,0,25,0"/>
                                <CheckBox x:Name="ChkBlockShorts" Content="Block YT Shorts" Style="{StaticResource ToggleSwitch}" Margin="0,0,0,0"/>
                            </WrapPanel>
                        </StackPanel>
                    </Border>

                    <Grid Height="360">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1*"/>
                            <ColumnDefinition Width="12"/>
                            <ColumnDefinition Width="1*"/>
                            <ColumnDefinition Width="12"/>
                            <ColumnDefinition Width="1*"/>
                        </Grid.ColumnDefinitions>

                        <Border Grid.Column="0" Background="White" CornerRadius="10" BorderBrush="#E2E8F0" BorderThickness="1" Padding="12">
                            <Border.Effect><DropShadowEffect BlurRadius="8" ShadowDepth="1" Opacity="0.05"/></Border.Effect>
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                
                                <StackPanel Grid.Row="0">
                                    <TextBlock Text="Block Apps (e.g., vlc.exe)" FontSize="13" FontWeight="Bold" Foreground="#64748B" Margin="0,0,0,6"/>
                                    <Grid Margin="0,0,0,8">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="70"/>
                                            <ColumnDefinition Width="55"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBox x:Name="TxtAppBlock" Grid.Column="0" Margin="0,0,5,0"/>
                                        <ComboBox x:Name="ComboAppBlock" Grid.Column="1" Margin="0,0,5,0"/>
                                        <Button x:Name="BtnAddAppBlock" Grid.Column="2" Content="ADD" Style="{StaticResource CareBtn}" Background="#3B82F6" Padding="5,4" FontSize="12" Click="BtnAddAppBlock_Click"/>
                                    </Grid>
                                </StackPanel>
                                <Grid Grid.Row="1" Margin="0,0,0,12">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="*"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <ListBox x:Name="ListAppBlock" Grid.Row="0" Margin="0,0,0,6"/>
                                    <Button x:Name="BtnRemAppBlock" Grid.Row="1" Content="Remove Selected" Style="{StaticResource CareBtn}" Background="#64748B" Padding="5,4" FontSize="12" Click="BtnRemAppBlock_Click"/>
                                </Grid>

                                <StackPanel Grid.Row="2">
                                    <TextBlock Text="Block Websites" FontSize="13" FontWeight="Bold" Foreground="#64748B" Margin="0,0,0,6"/>
                                    <Grid Margin="0,0,0,8">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="70"/>
                                            <ColumnDefinition Width="55"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBox x:Name="TxtWebBlock" Grid.Column="0" Margin="0,0,5,0"/>
                                        <ComboBox x:Name="ComboWebBlock" Grid.Column="1" Margin="0,0,5,0"/>
                                        <Button x:Name="BtnAddWebBlock" Grid.Column="2" Content="ADD" Style="{StaticResource CareBtn}" Background="#3B82F6" Padding="5,4" FontSize="12" Click="BtnAddWebBlock_Click"/>
                                    </Grid>
                                </StackPanel>
                                <Grid Grid.Row="3">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="*"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <ListBox x:Name="ListWebBlock" Grid.Row="0" Margin="0,0,0,6"/>
                                    <Button x:Name="BtnRemWebBlock" Grid.Row="1" Content="Remove Selected" Style="{StaticResource CareBtn}" Background="#64748B" Padding="5,4" FontSize="12" Click="BtnRemWebBlock_Click"/>
                                </Grid>
                            </Grid>
                        </Border>

                        <Border Grid.Column="2" Background="White" CornerRadius="10" BorderBrush="#E2E8F0" BorderThickness="1" Padding="12">
                            <Border.Effect><DropShadowEffect BlurRadius="8" ShadowDepth="1" Opacity="0.05"/></Border.Effect>
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <TextBlock Grid.Row="0" Text="Running Apps (Auto-Detect)" FontSize="13" FontWeight="Bold" Foreground="#64748B" Margin="0,0,0,6"/>
                                <Button x:Name="BtnAddFromRunning" Grid.Row="1" Content="Add Selected App to Active List ➡️" Style="{StaticResource CareBtn}" Background="#8B5CF6" Padding="5,8" Margin="0,0,0,10" Click="BtnAddFromRunning_Click"/>
                                <ListBox Grid.Row="2" x:Name="ListRunningApps"/>
                            </Grid>
                        </Border>

                        <Border Grid.Column="4" Background="White" CornerRadius="10" BorderBrush="#E2E8F0" BorderThickness="1" Padding="12">
                            <Border.Effect><DropShadowEffect BlurRadius="8" ShadowDepth="1" Opacity="0.05"/></Border.Effect>
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                
                                <StackPanel Grid.Row="0">
                                    <TextBlock Text="Allow Apps (e.g., code.exe)" FontSize="13" FontWeight="Bold" Foreground="#64748B" Margin="0,0,0,6"/>
                                    <Grid Margin="0,0,0,8">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="70"/>
                                            <ColumnDefinition Width="55"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBox x:Name="TxtAppAllow" Grid.Column="0" Margin="0,0,5,0"/>
                                        <ComboBox x:Name="ComboAppAllow" Grid.Column="1" Margin="0,0,5,0"/>
                                        <Button x:Name="BtnAddAppAllow" Grid.Column="2" Content="ADD" Style="{StaticResource CareBtn}" Background="#10B981" Padding="5,4" FontSize="12" Click="BtnAddAppAllow_Click"/>
                                    </Grid>
                                </StackPanel>
                                <Grid Grid.Row="1" Margin="0,0,0,12">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="*"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <ListBox x:Name="ListAppAllow" Grid.Row="0" Margin="0,0,0,6"/>
                                    <Button x:Name="BtnRemAppAllow" Grid.Row="1" Content="Remove Selected" Style="{StaticResource CareBtn}" Background="#64748B" Padding="5,4" FontSize="12" Click="BtnRemAppAllow_Click"/>
                                </Grid>

                                <StackPanel Grid.Row="2">
                                    <TextBlock Text="Allow Websites" FontSize="13" FontWeight="Bold" Foreground="#64748B" Margin="0,0,0,6"/>
                                    <Grid Margin="0,0,0,8">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="70"/>
                                            <ColumnDefinition Width="55"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBox x:Name="TxtWebAllow" Grid.Column="0" Margin="0,0,5,0"/>
                                        <ComboBox x:Name="ComboWebAllow" Grid.Column="1" Margin="0,0,5,0"/>
                                        <Button x:Name="BtnAddWebAllow" Grid.Column="2" Content="ADD" Style="{StaticResource CareBtn}" Background="#10B981" Padding="5,4" FontSize="12" Click="BtnAddWebAllow_Click"/>
                                    </Grid>
                                </StackPanel>
                                <Grid Grid.Row="3">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="*"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <ListBox x:Name="ListWebAllow" Grid.Row="0" Margin="0,0,0,6"/>
                                    <Button x:Name="BtnRemWebAllow" Grid.Row="1" Content="Remove Selected" Style="{StaticResource CareBtn}" Background="#64748B" Padding="5,4" FontSize="12" Click="BtnRemWebAllow_Click"/>
                                </Grid>
                            </Grid>
                        </Border>
                    </Grid>

                    <Border Background="#EEF2FF" CornerRadius="8" Padding="12" Margin="0,15,0,0" BorderBrush="#C7D2FE" BorderThickness="1">
                        <TextBlock x:Name="LblAdminMsg" Text="Admin Notice: Everything is running smoothly. Stay focused!" Foreground="#4F46E5" FontWeight="Bold" FontSize="14" TextWrapping="Wrap"/>
                    </Border>
                </StackPanel>

                <Grid x:Name="PageEyeCure" Grid.Row="1" Margin="40,20,40,40" Visibility="Collapsed">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <StackPanel Grid.Row="0" Margin="0,0,0,30">
                        <TextBlock Text="💻 Display &amp; Eye Cure Settings" FontSize="26" FontWeight="Bold" Foreground="{StaticResource PrimaryTeal}" Margin="0,0,0,10"/>
                        <TextBlock Text="Protect your eyes from blue light and screen glare during long focus sessions." FontSize="15" Foreground="#64748B"/>
                    </StackPanel>

                    <Border Grid.Row="1" Background="White" CornerRadius="12" Padding="30" BorderBrush="#E2E8F0" BorderThickness="1" VerticalAlignment="Top">
                        <Border.Effect><DropShadowEffect BlurRadius="15" ShadowDepth="2" Opacity="0.05"/></Border.Effect>
                        <StackPanel>
                            <TextBlock Text="Screen Brightness:" FontSize="16" FontWeight="Bold" Foreground="#1E293B" Margin="0,0,0,15"/>
                            <Slider x:Name="SliderBrightness" Minimum="10" Maximum="100" Value="100" TickFrequency="5" IsSnapToTickEnabled="True" Margin="0,0,0,30"/>

                            <TextBlock Text="Warmth (Blue Light Filter):" FontSize="16" FontWeight="Bold" Foreground="#1E293B" Margin="0,0,0,15"/>
                            <Slider x:Name="SliderWarmth" Minimum="0" Maximum="100" Value="0" TickFrequency="5" IsSnapToTickEnabled="True" Margin="0,0,0,40"/>

                            <TextBlock Text="Quick Presets:" FontSize="16" FontWeight="Bold" Foreground="#1E293B" Margin="0,0,0,15"/>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                                <Button Content="☀️ Day Mode (Normal)" Style="{StaticResource CareBtn}" Background="#3B82F6" Padding="20,12" Margin="0,0,15,0" />
                                <Button Content="📖 Reading Mode" Style="{StaticResource CareBtn}" Background="#10B981" Padding="20,12" Margin="0,0,15,0" />
                                <Button Content="🌙 Night Mode (Warm)" Style="{StaticResource CareBtn}" Background="#F59E0B" Padding="20,12" />
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </Grid>
                
                <Grid x:Name="PagePomodoro" Grid.Row="1" Margin="40,20,40,40" Visibility="Collapsed">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <StackPanel Grid.Column="0" Margin="0,0,30,0">
                        <TextBlock Text="🍅 Pomodoro Timer" FontSize="26" FontWeight="Bold" Foreground="{StaticResource PrimaryTeal}" Margin="0,0,0,10"/>
                        <TextBlock Text="The Pomodoro Technique is a time management method. Work for a set time, then take a short break." FontSize="15" Foreground="#64748B" TextWrapping="Wrap" Margin="0,0,0,30"/>
                        
                        <Border Background="White" CornerRadius="12" Padding="25" BorderBrush="#E2E8F0" BorderThickness="1">
                            <Border.Effect><DropShadowEffect BlurRadius="15" ShadowDepth="2" Opacity="0.05"/></Border.Effect>
                            <StackPanel>
                                <TextBlock Text="Focus Length (Minutes):" FontSize="15" FontWeight="Bold" Foreground="#1E293B" Margin="0,0,0,10"/>
                                <TextBox x:Name="TxtPomoMin" Text="25" Width="120" HorizontalAlignment="Left" Margin="0,0,0,20"/>
                                
                                <TextBlock Text="Total Sessions:" FontSize="15" FontWeight="Bold" Foreground="#1E293B" Margin="0,0,0,10"/>
                                <TextBox x:Name="TxtPomoSessions" Text="4" Width="120" HorizontalAlignment="Left" Margin="0,0,0,30"/>

                                <StackPanel Orientation="Horizontal">
                                    <Button Content="START POMODORO" Style="{StaticResource CareBtn}" Background="#10B981" Padding="20,12" Margin="0,0,15,0" Click="BtnPomoStart_Click"/>
                                    <Button Content="STOP" Style="{StaticResource CareBtn}" Background="#EF4444" Padding="20,12" Click="BtnPomoStop_Click"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>
                    </StackPanel>

                    <Border Grid.Column="1" Background="#0F172A" CornerRadius="15" Padding="30" VerticalAlignment="Top">
                        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                            <TextBlock Text="CURRENT SESSION" Foreground="#94A3B8" FontSize="16" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                            <TextBlock x:Name="LblPomoBigTime" Text="25:00" Foreground="White" FontSize="70" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,20"/>
                            <TextBlock x:Name="LblPomoBigStatus" Text="Session 1 of 4 | Focus Mode Active" Foreground="#38BDF8" FontSize="16" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                </Grid>

                <Grid x:Name="PageSettings" Grid.Row="1" Margin="40,20,40,40" Visibility="Collapsed">
                    <StackPanel>
                        <TextBlock Text="⚙️ App Settings" FontSize="26" FontWeight="Bold" Foreground="{StaticResource PrimaryTeal}" Margin="0,0,0,10"/>
                        <TextBlock Text="Manage your application behavior and startup preferences." FontSize="15" Foreground="#64748B" Margin="0,0,0,30"/>

                        <Border Background="White" CornerRadius="12" Padding="25" BorderBrush="#E2E8F0" BorderThickness="1" HorizontalAlignment="Left" Width="500">
                            <Border.Effect><DropShadowEffect BlurRadius="15" ShadowDepth="2" Opacity="0.05"/></Border.Effect>
                            <StackPanel>
                                <CheckBox Content="Start RasFocus automatically with Windows" Style="{StaticResource ToggleSwitch}" Margin="0,0,0,20"/>
                                <CheckBox Content="Enable Focus Background Sound (White Noise)" Style="{StaticResource ToggleSwitch}" Margin="0,0,0,20"/>
                                <CheckBox Content="Show System Tray Notifications" Style="{StaticResource ToggleSwitch}" Margin="0,0,0,30"/>
                                
                                <Button Content="📖 Read User Guide &amp; Documentation" Style="{StaticResource CareBtn}" Background="#3B82F6" Padding="15,10" HorizontalAlignment="Left"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Grid>

                <Grid x:Name="PageLiveChat" Grid.Row="1" Margin="40,20,40,30" Visibility="Collapsed">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Margin="0,0,0,15">
                        <TextBlock Text="💬 Live Chat Support" FontSize="26" FontWeight="Bold" Foreground="{StaticResource PrimaryTeal}" Margin="0,0,0,5"/>
                        <TextBlock Text="Talk directly with the developer or admin if you need to unlock early or face any issues." FontSize="15" Foreground="#64748B"/>
                    </StackPanel>

                    <Border Grid.Row="1" Background="White" CornerRadius="10" BorderBrush="#E2E8F0" BorderThickness="1" Margin="0,0,0,15">
                        <TextBox x:Name="TxtChatLog" IsReadOnly="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" BorderThickness="0" Padding="15" FontSize="15" Background="Transparent" Text="Admin: Welcome to RasFocus Pro Live Chat! How can I help you today?&#x0a;"/>
                    </Border>

                    <Grid Grid.Row="2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="120"/>
                        </Grid.ColumnDefinitions>
                        <TextBox x:Name="TxtChatInput" Grid.Column="0" Margin="0,0,15,0" Height="45" FontSize="15" Padding="10,0"/>
                        <Button x:Name="BtnSendChat" Grid.Column="1" Content="SEND MESSAGE" Style="{StaticResource CareBtn}" Background="#EC4899" Height="45"/>
                    </Grid>
                </Grid>

                <Grid x:Name="PageActivatePro" Grid.Row="1" Margin="40,20,40,40" Visibility="Collapsed">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1.2*"/>
                        <ColumnDefinition Width="1*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Margin="0,0,40,0">
                        <TextBlock Text="⭐ Upgrade to Premium" FontSize="26" FontWeight="Bold" Foreground="{StaticResource PrimaryTeal}" Margin="0,0,0,10"/>
                        <TextBlock Text="Get unlimited access to all focus features, custom block lists, and advanced adult filters. Send payment via bKash/Nagad to 01566054963." FontSize="15" Foreground="#64748B" TextWrapping="Wrap" Margin="0,0,0,30"/>

                        <Border Background="White" CornerRadius="12" Padding="25" BorderBrush="#E2E8F0" BorderThickness="1">
                            <Border.Effect><DropShadowEffect BlurRadius="15" ShadowDepth="2" Opacity="0.05"/></Border.Effect>
                            <StackPanel>
                                <TextBlock Text="Your Name or Email:" FontSize="14" FontWeight="Bold" Foreground="#1E293B" Margin="0,0,0,8"/>
                                <TextBox x:Name="TxtUpgradeEmail" Height="35" Margin="0,0,0,15"/>

                                <TextBlock Text="Sender Phone Number (bKash/Nagad):" FontSize="14" FontWeight="Bold" Foreground="#1E293B" Margin="0,0,0,8"/>
                                <TextBox x:Name="TxtUpgradePhone" Height="35" Margin="0,0,0,15"/>

                                <TextBlock Text="Transaction ID (TrxID):" FontSize="14" FontWeight="Bold" Foreground="#1E293B" Margin="0,0,0,8"/>
                                <TextBox x:Name="TxtUpgradeTrx" Height="35" Margin="0,0,0,15"/>

                                <TextBlock Text="Select Package:" FontSize="14" FontWeight="Bold" Foreground="#1E293B" Margin="0,0,0,8"/>
                                <ComboBox x:Name="ComboPackages" Height="35" Margin="0,0,0,25">
                                    <ComboBoxItem IsSelected="True">6 Months (50 BDT)</ComboBoxItem>
                                    <ComboBoxItem>1 Year (100 BDT)</ComboBoxItem>
                                    <ComboBoxItem>Lifetime Access (250 BDT)</ComboBoxItem>
                                </ComboBox>

                                <Button Content="SUBMIT UPGRADE REQUEST" Style="{StaticResource CareBtn}" Background="#10B981" Padding="15,12"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>

                    <Border Grid.Column="1" Background="#F8FAFC" CornerRadius="12" Padding="25" BorderBrush="#E2E8F0" BorderThickness="1" VerticalAlignment="Top">
                        <StackPanel>
                            <TextBlock Text="Why Upgrade?" FontSize="18" FontWeight="Bold" Foreground="#1E293B" Margin="0,0,0,20"/>
                            <TextBlock Text="✅ Unlimited Custom App Blocking" FontSize="15" Margin="0,0,0,10" Foreground="#475569"/>
                            <TextBlock Text="✅ Unlimited Website Blocking" FontSize="15" Margin="0,0,0,10" Foreground="#475569"/>
                            <TextBlock Text="✅ Advanced Adult Image/Video Filter" FontSize="15" Margin="0,0,0,10" Foreground="#475569"/>
                            <TextBlock Text="✅ Developer Live Chat Support" FontSize="15" Margin="0,0,0,10" Foreground="#475569"/>
                            <TextBlock Text="✅ Priority Cloud Sync &amp; Backup" FontSize="15" Margin="0,0,0,10" Foreground="#475569"/>
                            <TextBlock Text="✅ Remove Ads &amp; Watermarks" FontSize="15" Margin="0,0,0,10" Foreground="#475569"/>
                        </StackPanel>
                    </Border>
                </Grid>

            </Grid>
        </Grid>
    </Border>
</Window>
