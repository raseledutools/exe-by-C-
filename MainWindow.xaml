<Window x:Class="RasFocusPro.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="RasFocus Pro" Height="720" Width="1100"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen" MouseLeftButtonDown="Window_MouseLeftButtonDown"
        TextOptions.TextFormattingMode="Display" TextOptions.TextRenderingMode="ClearType">

    <Window.Resources>
        <SolidColorBrush x:Key="PrimaryTeal" Color="#14B8C8"/>
        <SolidColorBrush x:Key="DarkText" Color="#333333"/>
        <SolidColorBrush x:Key="LightBg" Color="#FFFFFF"/>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource LightBg}"/>
            <Setter Property="Foreground" Value="{StaticResource DarkText}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="BorderBrush" Value="#CBD5E1"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border CornerRadius="4" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="0"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter Property="BorderBrush" Value="{StaticResource PrimaryTeal}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Button" x:Key="CareBtn">
            <Setter Property="Background" Value="{StaticResource PrimaryTeal}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6" Padding="12,6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Opacity" Value="0.85"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="CheckBox" x:Key="ToggleSwitch">
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <StackPanel Orientation="Horizontal">
                            <Grid Width="40" Height="20">
                                <Border x:Name="BgBorder" CornerRadius="10" Background="#D1D5DB"/>
                                <Ellipse x:Name="Thumb" Fill="White" Width="16" Height="16" HorizontalAlignment="Left" Margin="2">
                                    <Ellipse.Effect>
                                        <DropShadowEffect BlurRadius="4" ShadowDepth="1" Opacity="0.2"/>
                                    </Ellipse.Effect>
                                </Ellipse>
                            </Grid>
                            <ContentPresenter Margin="8,0,0,0" VerticalAlignment="Center" TextBlock.FontSize="13" TextBlock.Foreground="{StaticResource DarkText}"/>
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="BgBorder" Property="Background" Value="{StaticResource PrimaryTeal}"/>
                                <Setter TargetName="Thumb" Property="HorizontalAlignment" Value="Right"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="#F8FAFC" CornerRadius="10" Margin="15">
        <Border.Effect>
            <DropShadowEffect BlurRadius="15" ShadowDepth="3" Opacity="0.2" Color="Black"/>
        </Border.Effect>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="220"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="{StaticResource PrimaryTeal}" CornerRadius="10,0,0,10">
                <StackPanel Margin="0,20,0,0">
                    <StackPanel Orientation="Horizontal" Margin="20,0,0,30">
                        <TextBlock Text="👁️" FontSize="24" Foreground="White" Margin="0,0,10,0"/>
                        <TextBlock Text="RasFocus Pro" Foreground="White" FontSize="18" FontWeight="Bold" VerticalAlignment="Center"/>
                    </StackPanel>

                    <ListBox x:Name="SidebarList" Background="Transparent" BorderThickness="0" Foreground="White" FontSize="15" FontWeight="SemiBold" SelectionChanged="SidebarList_SelectionChanged">
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="Padding" Value="20,15"/>
                                <Setter Property="Cursor" Value="Hand"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListBoxItem">
                                            <Border x:Name="BorderBg" Background="Transparent">
                                                <ContentPresenter Margin="{TemplateBinding Padding}"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsSelected" Value="True">
                                                    <Setter TargetName="BorderBg" Property="Background" Value="#F8FAFC"/>
                                                    <Setter Property="Foreground" Value="{StaticResource PrimaryTeal}"/>
                                                </Trigger>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="BorderBg" Property="Background" Value="#17C3D4"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListBox.ItemContainerStyle>
                        <ListBoxItem IsSelected="True">🛡️  Focus Mode</ListBoxItem>
                        <ListBoxItem>💻  Display / Eye Cure</ListBoxItem>
                        <ListBoxItem>☕  Pomodoro Break</ListBoxItem>
                        <ListBoxItem>⚙️  Settings</ListBoxItem>
                        <ListBoxItem>💬  Live Chat</ListBoxItem>
                        <ListBoxItem>⭐  Activate Pro</ListBoxItem>
                    </ListBox>
                </StackPanel>
            </Border>

            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="40"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="10,10,10,0">
                    <Button Content="—" Background="Transparent" Foreground="#888" FontSize="16" BorderThickness="0" Width="30" Height="30" Cursor="Hand" Click="CloseButton_Click"/>
                    <Button Content="✕" Background="Transparent" Foreground="#888" FontSize="16" BorderThickness="0" Width="30" Height="30" Cursor="Hand" Click="CloseButton_Click"/>
                </StackPanel>

                <StackPanel x:Name="PageFocusMode" Grid.Row="1" Margin="25,0,25,20" Visibility="Visible">
                    
                    <Grid Margin="0,0,0,15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Orientation="Horizontal">
                            <TextBlock Text="Profile Name:" VerticalAlignment="Center" FontSize="13" Foreground="#555" Margin="0,0,8,0"/>
                            <TextBox x:Name="TxtProfileName" Width="120" Margin="0,0,10,0" Text="Rasel Mia"/>
                            <Button Content="SAVE" Style="{StaticResource CareBtn}" Background="#10B981" Padding="12,4" FontSize="12" Click="BtnSave_Click"/>
                        </StackPanel>

                        <TextBlock Grid.Column="1" Text="TRIAL: 174 DAYS LEFT" Foreground="#F59E0B" FontWeight="Bold" FontSize="13" VerticalAlignment="Center" HorizontalAlignment="Center"/>

                        <StackPanel Grid.Column="2" Orientation="Horizontal">
                            <Button Content="LIVE CHAT" Style="{StaticResource CareBtn}" Background="#EC4899" Margin="0,0,10,0"/>
                            <Button Content="UPGRADE" Style="{StaticResource CareBtn}" Background="#F59E0B"/>
                        </StackPanel>
                    </Grid>

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                        <TextBlock Text="Pass:" VerticalAlignment="Center" FontSize="13" Foreground="#555" Margin="0,0,5,0"/>
                        <TextBox x:Name="TxtPass" Width="80" Margin="0,0,15,0"/>

                        <TextBlock Text="Time (Hr:Min):" VerticalAlignment="Center" FontSize="13" Foreground="#555" Margin="0,0,5,0"/>
                        <TextBox Width="40" Margin="0,0,5,0" Text="0"/>
                        <TextBox Width="40" Margin="0,0,15,0" Text="0"/>

                        <Button Content="START" Style="{StaticResource CareBtn}" Background="#10B981" Margin="0,0,8,0" Click="BtnStart_Click"/>
                        <Button Content="STOP" Style="{StaticResource CareBtn}" Background="#64748B" Margin="0,0,8,0" Click="BtnStop_Click"/>
                        <Button Content="STOP WATCH" Style="{StaticResource CareBtn}" Background="#0F172A" Margin="0,0,8,0"/>
                        <Button Content="POMODORO" Style="{StaticResource CareBtn}" Background="#EF4444" Margin="0,0,8,0"/>
                        <Button Content="EYE CURE" Style="{StaticResource CareBtn}" Background="#8B5CF6" Margin="0,0,15,0"/>

                        <TextBlock x:Name="LblStatus" Text="Ready" Foreground="#EF4444" FontWeight="Bold" VerticalAlignment="Center"/>
                    </StackPanel>

                    <WrapPanel Margin="0,0,0,15">
                        <RadioButton Content="Block List" IsChecked="True" Foreground="#333" FontWeight="Bold" Margin="0,0,15,0" VerticalAlignment="Center"/>
                        <RadioButton Content="Allow List" Foreground="#333" FontWeight="Bold" Margin="0,0,25,0" VerticalAlignment="Center"/>
                        
                        <CheckBox Content="AD BLOCKER" Style="{StaticResource ToggleSwitch}" Margin="0,0,20,0"/>
                        <CheckBox Content="Block FB Reels" Style="{StaticResource ToggleSwitch}" Margin="0,0,20,0"/>
                        <CheckBox Content="Block YT Shorts" Style="{StaticResource ToggleSwitch}" Margin="0,0,0,0"/>
                    </WrapPanel>

                    <Grid Height="390">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1*"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="1*"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="1*"/>
                        </Grid.ColumnDefinitions>

                        <Border Grid.Column="0" Background="White" CornerRadius="6" BorderBrush="#E2E8F0" BorderThickness="1" Padding="10">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                
                                <TextBlock Grid.Row="0" Text="Block Apps (vlc.exe)" FontSize="12" Foreground="#555" Margin="0,0,0,5"/>
                                <Grid Grid.Row="1" Margin="0,0,0,10">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBox Grid.Column="0" x:Name="TxtAppBlock" Margin="0,0,5,0"/>
                                    <Button Grid.Column="1" Content="ADD" Style="{StaticResource CareBtn}" Background="#2563EB" Padding="10,2" FontSize="12"/>
                                    <ListBox Grid.Column="0" Grid.ColumnSpan="2" Margin="0,35,0,0" BorderBrush="#CBD5E1"/>
                                </Grid>

                                <TextBlock Grid.Row="2" Text="Block Websites" FontSize="12" Foreground="#555" Margin="0,0,0,5"/>
                                <Grid Grid.Row="3" Margin="0,0,0,10">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBox Grid.Column="0" x:Name="TxtWebBlock" Margin="0,0,5,0"/>
                                    <Button Grid.Column="1" Content="ADD" Style="{StaticResource CareBtn}" Background="#2563EB" Padding="10,2" FontSize="12"/>
                                    <ListBox Grid.Column="0" Grid.ColumnSpan="2" Margin="0,35,0,0" BorderBrush="#CBD5E1"/>
                                </Grid>

                                <Button Grid.Row="4" Content="Remove" Style="{StaticResource CareBtn}" Background="#2563EB" Padding="5"/>
                            </Grid>
                        </Border>

                        <Border Grid.Column="2" Background="White" CornerRadius="6" BorderBrush="#E2E8F0" BorderThickness="1" Padding="10">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <TextBlock Grid.Row="0" Text="Running Apps (Auto-Detect)" FontSize="12" Foreground="#555" Margin="0,0,0,5"/>
                                <Button Grid.Row="1" Content="Add Selected App to List" Style="{StaticResource CareBtn}" Background="#2563EB" Padding="5" Margin="0,0,0,10"/>
                                <ListBox Grid.Row="2" x:Name="ListRunningApps" BorderBrush="#CBD5E1"/>
                            </Grid>
                        </Border>

                        <Border Grid.Column="4" Background="White" CornerRadius="6" BorderBrush="#E2E8F0" BorderThickness="1" Padding="10">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                
                                <TextBlock Grid.Row="0" Text="Allow Apps (code.exe)" FontSize="12" Foreground="#555" Margin="0,0,0,5"/>
                                <Grid Grid.Row="1" Margin="0,0,0,10">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBox Grid.Column="0" x:Name="TxtAppAllow" Margin="0,0,5,0"/>
                                    <Button Grid.Column="1" Content="ADD" Style="{StaticResource CareBtn}" Background="#2563EB" Padding="10,2" FontSize="12"/>
                                    <ListBox Grid.Column="0" Grid.ColumnSpan="2" Margin="0,35,0,0" BorderBrush="#CBD5E1"/>
                                </Grid>

                                <TextBlock Grid.Row="2" Text="Allow Webs" FontSize="12" Foreground="#555" Margin="0,0,0,5"/>
                                <Grid Grid.Row="3" Margin="0,0,0,10">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBox Grid.Column="0" x:Name="TxtWebAllow" Margin="0,0,5,0"/>
                                    <Button Grid.Column="1" Content="ADD" Style="{StaticResource CareBtn}" Background="#2563EB" Padding="10,2" FontSize="12"/>
                                    <ListBox Grid.Column="0" Grid.ColumnSpan="2" Margin="0,35,0,0" BorderBrush="#CBD5E1"/>
                                </Grid>

                                <Button Grid.Row="4" Content="Remove" Style="{StaticResource CareBtn}" Background="#2563EB" Padding="5"/>
                            </Grid>
                        </Border>
                    </Grid>
                </StackPanel>

                <StackPanel x:Name="PageEyeCure" Grid.Row="1" Margin="40,20,40,40" Visibility="Collapsed">
                    <TextBlock Text="Display Settings" FontSize="24" FontWeight="Bold" Foreground="{StaticResource PrimaryTeal}"/>
                </StackPanel>
                
                <StackPanel x:Name="PagePomodoro" Grid.Row="1" Margin="40,20,40,40" Visibility="Collapsed">
                    <TextBlock Text="Pomodoro Timer" FontSize="24" FontWeight="Bold" Foreground="{StaticResource PrimaryTeal}"/>
                </StackPanel>

            </Grid>
        </Grid>
    </Border>
</Window>
